# Modern UI preview

This directory contains the isolated Subsurface Mobile modern UI preview.

The preview is intentionally separate from the legacy QML screens so visual and interaction changes can be developed without replacing existing dive workflows. It is currently accessible from Help > About > Open Modern UI Preview.

Reusable design values live in `DesignTokens.qml`, shared components live in `components/`, and preview pages live in `pages/`.
